# Simple Linear Regression 

I have implemented simple linear regression in two ways .
1. From Scratch Using only numpy and pandas .
2. From Scikit learn 

I have successfully achieved a similar R2, RMSE , MSE score in both of them that means my scratch code works as fine as Scikit Learn code.
But the problem in my code has that it uses batch gradient descent which makes it very slow.
